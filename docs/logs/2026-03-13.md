# 작업 로그 — 2026-03-13

## 00:31 — 해지된 업체 재활성화 기능 추가

### 요약 (비개발자용)
> 관리자 페이지의 업체관리에서, 기존에는 업체를 "해지"만 할 수 있었고 다시 "활성"으로 되돌릴 수 없었습니다.
> 이번 작업으로 해지된 업체를 다시 활성화할 수 있는 버튼과 기능을 추가했습니다.
> 활성화하면 정산, 선물하기 등 모든 기능이 원래대로 정상 동작합니다.

### 작업 내용
- 업체 재활성화 API 엔드포인트 추가 (`PUT /api/admin/businesses/[businessId]`)
  - 이미 활성 상태인 업체는 중복 활성화 방지
  - 존재하지 않는 업체 예외 처리
- 업체 상세 모달 UI 수정
  - `terminated` 상태일 때 "업체 활성화" 버튼 표시 (초록색, RefreshCcw 아이콘)
  - `active` 상태일 때 기존 "업체 해지" 버튼 표시 (빨간색)
  - 확인 다이얼로그를 통한 2단계 확인 프로세스
- 전체 플로우 영향도 분석 수행
  - 선물하기, 회원 검색, 정산 관리/전체보기 등 6개 의존 지점 확인
  - 수신 계정은 영구 할당 방식이므로 엣지케이스 없음 확인

### 수정된 파일
| 파일 | 변경 요약 |
|------|----------|
| `src/app/api/admin/businesses/[businessId]/route.ts` | PUT 엔드포인트 추가 (terminated → active 상태 전환) |
| `src/components/admin/businesses/BusinessDetailModal.tsx` | 상태별 활성화/해지 버튼 분기 표시 |

### 에러 및 해결
없음

### Git
- **커밋**: `000ee77` feat: 해지된 업체 재활성화 기능 추가
- **Push**: ✅ 완료
- **브랜치**: master

### 문서 업데이트
해당 없음 (독립 개선 작업, 태스크 추적 파일 없음)

### 다음 작업
- 사용자 지시 대기

---

## 00:40 — 업체 수신 계정 SMS 발송 제거

### 요약 (비개발자용)
> 업체의 수신 계정으로 상품권을 선물하면, 핀 번호가 자동으로 재고로 돌아갑니다.
> 그런데 기존에는 수신 계정에도 SMS(문자)를 보내고 있었습니다.
> 핀 번호가 없는 빈 바우처 정보를 문자로 보내는 건 의미가 없으므로, 수신 계정일 때는 SMS를 보내지 않도록 수정했습니다.
> 일반 고객에게 선물할 때는 기존대로 SMS가 정상 발송됩니다.

### 작업 내용
- `process_gift` RPC의 `auto_recycled` 반환값을 활용하여 수신 계정 여부 판별
- `auto_recycled === true`일 때 SMS 발송 블록을 건너뜀
- 나머지 로직(바우처 생성, 핀 복원, gifts 기록, 자동 정산)은 모두 유지

### 수정된 파일
| 파일 | 변경 요약 |
|------|----------|
| `src/app/api/vouchers/[code]/gift/route.ts` | `if (!autoRecycled)` 조건으로 SMS 발송 분기 |

### 에러 및 해결
없음

### Git
- **커밋**: `5a940d5` fix: 업체 수신 계정 선물 시 불필요한 SMS 발송 제거
- **Push**: ✅ 완료
- **브랜치**: master

### 문서 업데이트
해당 없음

### 다음 작업
- 사용자 지시 대기

---

## 00:55 — 상품 구매 수량 최대 30개 제한 및 UI 개선

### 요약 (비개발자용)
> 기존에는 상품 상세 페이지에서 수량을 무제한으로 올릴 수 있었고, 결제 페이지에서만 최대 10개로 제한했습니다.
> 이번 작업으로 모든 곳에서 최대 30개로 통일했습니다.
> 또한 -10/+10 버튼을 추가해서 대량 구매 시 편리하게 수량을 조절할 수 있습니다.
> 수수료 별도를 선택하면 "핀 번호 확인 시 결제됩니다" 안내 문구도 추가했습니다.
> 모바일에서 뒤로가기 버튼 정렬이 어긋나는 문제도 함께 수정했습니다.

### 작업 내용
- 상품 상세 페이지: `handleIncrement`에 `Math.min(30)` 상한 추가, ±10 버튼 추가
- 주문 페이지: 수량 클램핑 `Math.min(10)` → `Math.min(30)` 변경
- 결제 API(ready): Zod 검증 `.max(10)` → `.max(30)` 변경
- "수량 선택 (최대 30개)" 라벨 표시
- 수수료 별도 선택 시 최종 결제금액 하단에 "핀 번호 확인 시 결제됩니다" 안내 추가
- 모바일 패딩 `px-6` → `px-4 sm:px-6`으로 변경 (뒤로가기 버튼 정렬 수정)

### 수정된 파일
| 파일 | 변경 요약 |
|------|----------|
| `src/app/(user)/product/[id]/ProductDetailClient.tsx` | ±10 버튼, 최대 30개 제한, 안내 문구, 모바일 패딩 수정 |
| `src/app/(user)/order/OrderPageClient.tsx` | 수량 클램핑 10 → 30 |
| `src/app/api/payment/ready/route.ts` | Zod 검증 max 10 → 30 |

### 에러 및 해결
없음

### Git
- **커밋**: `5156f8d` feat: 상품 구매 수량 최대 30개 제한 및 UI 개선
- **Push**: ✅ 완료
- **브랜치**: master

### 문서 업데이트
해당 없음

### 다음 작업
- 사용자 지시 대기

---

## 10:57 — 다날 본인인증(T-PAY UAS) 연동 준비

### 요약 (비개발자용)
> 회원가입, 아이디 찾기, 비밀번호 재설정에 필요한 휴대폰 본인인증 서비스(다날) 연동을 준비했습니다.
> 다날에서 받은 매뉴얼, 응답코드, 보안가이드, NodeJS 예제 코드를 분석하여 연동 흐름을 파악했습니다.
> 환경변수 파일(.env.local)에 인증 키를 저장할 구조를 만들었고, 사용자가 직접 값을 입력했습니다.
> 다날 측에 휴대폰번호 리턴값 추가 요청이 필요한 상태이며, 회신 후 실제 연동 작업을 진행할 예정입니다.

### 작업 내용
- 다날 본인인증 기술 문서 분석
  - `UAS_Manual.1.1.6.UI.doc` — 연동 매뉴얼 (CPID, CPPWD, 플로우 파악)
  - `response.2.4.xls` — 응답코드
  - `security.1.0.doc` — 보안가이드
- NodeJS 예제 코드 전체 분석 (`danal/UAS.UI.1.2.5.NODEJS/`)
  - `Ready.js`: ITEMSEND → TID 발급 흐름
  - `CPCGI.js`: CONFIRM → 인증 결과 수신 흐름
  - `inc/function.js`: CallTrans (axios POST to `uas.teledit.com`)
  - `server.js`: Express 라우팅 구조
- `.env.local` 파일 생성 (`DANAL_CPID`, `DANAL_CPPWD` 빈 값 구조)
- 다날 리턴값 추가 요청 사항 정리
  - 휴대폰번호: 필요 (회원가입 자동입력 + SMS 발송)
  - 통신사: 불필요
  - 내/외국인여부: 불필요

### 수정된 파일
| 파일 | 변경 요약 |
|------|----------|
| `.env.local` | 신규 생성 — 다날 CPID/CPPWD 환경변수 구조 |

### 에러 및 해결
없음

### Git
- **커밋**: 없음 (환경변수 파일은 커밋 대상 아님)
- **Push**: 해당 없음
- **브랜치**: master

### 문서 업데이트
해당 없음

### 다음 작업
- 다날 측 휴대폰번호 리턴값 적용 회신 대기
- 회신 완료 후 본인인증 연동 API 구현 (Next.js API Route + 프론트엔드 팝업)

---

## 12:44 — 다날 본인인증(T-PAY UAS) 연동 완료 및 보안 강화

### 요약 (비개발자용)
> 회원가입, 아이디 찾기, 비밀번호 재설정 3개 페이지에 휴대폰 본인인증 기능을 연동했습니다.
> 사용자가 "본인인증하기" 버튼을 누르면 팝업이 열리고, SMS 인증을 완료하면 결과가 자동으로 반영됩니다.
>
> 회원가입 시 이미 가입된 번호면 "이미 가입된 계정이 있습니다"와 마스킹된 아이디(예: wlst****)를 알려주고,
> 아이디 찾기로 안내합니다. 비밀번호 재설정은 일회용 토큰 방식으로 보안을 강화하여,
> 본인인증 없이는 비밀번호를 변경할 수 없도록 했습니다.
> 비밀번호 찾기에서 해당 번호로 가입된 계정이 없으면 에러 메시지와 함께 로그인 페이지로 안내합니다.
>
> 코드 리뷰를 2회 진행하여 XSS 방어, 빈값 검증, 중복 요청 방지 등 보안 이슈를 모두 수정했습니다.

### 작업 내용
- 다날 T-PAY UAS SMS 인증 API 전체 연동
  - Ready API: TID 발급 + 세션 생성 (`POST /api/auth/identity/ready`)
  - Callback API: 다날 인증 완료 후 Confirm + 세션 저장 (`POST /api/auth/identity/callback`)
  - Result API: 프론트엔드에서 결과 조회 + 세션 삭제 (`POST /api/auth/identity/result`)
- 프론트엔드 팝업 기반 인증 플로우 구현
  - `useIdentityVerification` 커스텀 훅 (Ready → 팝업 → postMessage → Result)
  - `IdentityVerificationModal` 모달 컴포넌트 (상태별 UI 분기)
- 회원가입: 기존 회원 감지 → 마스킹된 아이디 표시 + "아이디 찾기" 버튼
- 아이디 찾기: 본인인증 후 별도 API로 원본 아이디 + 가입일 표시
- 비밀번호 재설정: 일회용 `resetToken` 기반 검증 (name+phone만으로는 변경 불가)
- 비밀번호 찾기 계정 미발견: 에러 모달 + "확인" 버튼 → 로그인 페이지 이동
- 코드 리뷰 반영 (2회)
  - XSS 방어: `escapeHtml`, `escapeForJs`, postMessage `targetOrigin` 검증
  - Callback NAME/PHONE 빈값 검증 추가
  - Result API `purpose` 값 Zod 스키마 검증
  - 중복 인증 요청 방지 (`isLoadingRef` 기반 가드)
  - `maskUsername` 공용 유틸리티로 추출 (`lib/utils.ts`)
  - `excludeKeys`에 CPID 추가 (프론트 노출 방지)
  - 인메모리 세션 `globalThis` 저장 (핫 리로드 대응)
- PRD 업데이트: 마이페이지 휴대폰 번호 변경 기능 삭제

### 수정된 파일
| 파일 | 변경 요약 |
|------|----------|
| `src/lib/danal/client.ts` | 신규 — 다날 API 통신 모듈 (Ready, Confirm) |
| `src/lib/danal/session.ts` | 신규 — 인메모리 세션 + 비밀번호 재설정 토큰 관리 |
| `src/lib/danal/types.ts` | 신규 — 다날 연동 타입 정의 |
| `src/app/api/auth/identity/ready/route.ts` | 신규 — Ready API (TID 발급) |
| `src/app/api/auth/identity/callback/route.ts` | 신규 — Callback API (다날 → 서버) |
| `src/app/api/auth/identity/result/route.ts` | 신규 — Result API (결과 조회, Zod 검증) |
| `src/hooks/use-identity-verification.ts` | 신규 — 본인인증 커스텀 훅 |
| `src/components/ui/identity-verification-modal.tsx` | 모달 전면 리팩토링 (다날 연동, 상태 분기) |
| `src/app/(auth)/auth/register/page.tsx` | purpose="register" 추가 |
| `src/app/(auth)/auth/find-id/page.tsx` | purpose="find-id" 추가 |
| `src/app/(auth)/auth/reset-password/page.tsx` | resetToken 기반 비밀번호 변경, 마스킹 아이디 표시 |
| `src/app/api/auth/reset-password/route.ts` | name+phone → resetToken 기반 검증으로 변경 |
| `src/lib/validations/auth.ts` | serverResetPasswordSchema에 resetToken 필드 적용 |
| `src/lib/utils.ts` | `maskUsername` 공용 함수 추가 |
| `.env.example` | DANAL_CPID, DANAL_CPPWD 항목 추가 |
| `docs/PRD.md` | 마이페이지 휴대폰 번호 변경 기능 삭제 |

### 에러 및 해결
| 에러 | 원인 | 해결 |
|------|------|------|
| 아이디 중복확인 후 "사용중" 메시지 안 사라짐 | 성공 시 `setServerError("")` 누락 | 성공 분기에 에러 초기화 추가 |
| 본인인증 세션 만료 오류 (재인증 불가) | 핫 리로드 시 인메모리 Map 초기화 | `globalThis`에 저장하여 유지 |
| 아이디 찾기에서 마스킹된 아이디 표시 | register 전용 기존 회원 체크가 모든 purpose에 적용 | `purpose` 파라미터로 분기 |
| reset-password API 본인인증 우회 가능 | name+phone만으로 비밀번호 변경 가능 | 일회용 resetToken 검증 방식으로 변경 |
| 프로덕션 "서버 오류 발생" | Vercel 환경변수 미설정 | NEXT_PUBLIC_APP_URL, DANAL_CPID, DANAL_CPPWD 확인 필요 |

### Git
- **커밋**: `f8f0d01` feat: 다날 본인인증(T-PAY UAS) 연동 및 보안 강화
- **Push**: ✅ 완료
- **브랜치**: master

### 문서 업데이트
해당 없음 (TASKS.md, ROADMAP.md 삭제된 상태)

### 다음 작업
- Vercel 환경변수 확인 (프로덕션 서버 오류 해결)
- 인메모리 세션 → Redis 교체 (프로덕션 배포 전 필수)
- 회원정보 페이지에서 회원가입 버튼 재클릭 시 회원정보 페이지에 머무는 버그 수정

---

## 16:07 — 이메일 인증 제거 및 로그인 불가 버그 수정

### 요약 (비개발자용)
> 모든 회원이 로그인할 때 "비밀번호가 틀립니다"라는 오류가 발생하는 문제를 해결했습니다.
> 원인은 Supabase(회원 관리 서비스) 설정에서 "이메일 인증" 기능이 켜져 있었기 때문입니다.
> 이 프로젝트는 휴대폰 본인인증(다날)을 사용하므로 이메일 인증이 필요 없습니다.
> 코드에서 이메일 인증 관련 로직을 모두 제거하고, Supabase 대시보드에서도 설정을 끄고,
> 기존 회원들의 이메일 인증 상태를 일괄 처리하여 모든 회원이 정상 로그인 가능하도록 했습니다.

### 작업 내용
- 회원가입 API: `supabase.auth.signUp()` → `adminClient.auth.admin.createUser({ email_confirm: true })`로 변경
  - 이메일 인증을 거치지 않고 가입 즉시 로그인 가능
- 이메일 인증 관련 코드 제거
  - `hasSession` 분기 로직 삭제
  - "이메일 인증을 완료해 주세요" 메시지 삭제
  - 불필요한 `createClient` import 제거
- 로그인 API에 Supabase Auth 에러 로깅 추가 (디버깅용)
- Supabase 대시보드 설정 변경 (사용자 직접 수행)
  - Authentication → Providers → Email → "Confirm email" 비활성화
  - SQL: `UPDATE auth.users SET email_confirmed_at = NOW() WHERE email_confirmed_at IS NULL;`

### 수정된 파일
| 파일 | 변경 요약 |
|------|----------|
| `src/app/api/auth/register/route.ts` | signUp → admin.createUser 변경, 이메일 인증 로직 제거 |
| `src/app/api/auth/login/route.ts` | Supabase Auth 에러 로깅 추가 |

### 에러 및 해결
| 에러 | 원인 | 해결 |
|------|------|------|
| 모든 회원 로그인 실패 ("비밀번호 틀림") | Supabase "Confirm email" 설정이 활성화되어 이메일 미인증 회원 로그인 차단 | 코드에서 admin.createUser(email_confirm: true) 사용 + 대시보드 설정 비활성화 + 기존 회원 일괄 인증 처리 |

### Git
- **커밋**: `7b3da63` fix: 이메일 인증 제거 및 회원가입 즉시 활성화
- **Push**: ✅ 완료
- **브랜치**: master

### 문서 업데이트
해당 없음

### 다음 작업
- 사용자 지시 대기

---

## 23:57 — 관리자 업체 목록 API 500 에러 수정

### 요약 (비개발자용)
> 관리자 페이지에서 업체 목록이 표시되지 않는 문제를 해결했습니다.
> 원인은 두 가지였습니다:
> 1. 데이터베이스에 "SMS 인증 전화번호" 컬럼이 아직 추가되지 않은 상태에서 코드가 그 컬럼을 조회하려고 해서 에러가 발생했습니다. → 사용자가 직접 마이그레이션을 적용하여 해결
> 2. 업체 테이블이 회원 테이블을 두 번 참조(업체 회원 + 수신 계정)하면서 데이터베이스 조회 시 혼란이 생길 수 있어, 안전한 방식(별도 조회)으로 변경했습니다.
>
> 추가로, 외부 SDK 샘플 코드(다날)가 린트 검사에 포함되어 불필요한 에러가 표시되던 문제도 해결했습니다.

### 작업 내용
- **원인 분석**: Playwright 브라우저 테스트로 Network 요청 확인 → 500 에러 확인 → API에 임시 에러 상세 추가 → `column businesses.auth_phone does not exist` 에러 특정
- **마이그레이션 적용 안내**: `20260313000006_add_auth_phone_to_businesses.sql` 파일을 사용자에게 전달하여 Supabase에서 직접 실행
- **FK 모호성 방지**: `users!businesses_user_id_fkey` PostgREST JOIN을 별도 쿼리로 분리
  - 목록 조회: user_id 벌크 조회 → `userMap`으로 매핑
  - 상세 조회: user_id 단건 조회
- **ESLint 설정 개선**: `danal/` 폴더를 `globalIgnores`에 추가 (외부 SDK 코드 린트 제외)

### 수정된 파일
| 파일 | 변경 요약 |
|------|----------|
| `src/app/api/admin/businesses/route.ts` | FK JOIN 제거 → 별도 user 벌크 쿼리로 변경 |
| `src/app/api/admin/businesses/[businessId]/route.ts` | FK JOIN 제거 → 별도 user 단건 쿼리로 변경 |
| `eslint.config.mjs` | `danal/**` globalIgnores 추가 |

### 에러 및 해결
| 에러 | 원인 | 해결 |
|------|------|------|
| 500 QUERY_ERROR: `column businesses.auth_phone does not exist` | `auth_phone` 컬럼 마이그레이션이 DB에 미적용 | 사용자가 Supabase SQL Editor에서 마이그레이션 직접 실행 |
| PostgREST FK 모호성 가능성 | `businesses` → `users` FK가 2개 (user_id, receiving_account_id) | FK JOIN 대신 별도 쿼리로 user 정보 조회 |
| ESLint 14 errors, 6 warnings | `danal/` 외부 SDK 코드가 린트 대상에 포함 | `globalIgnores`에 `danal/**` 추가 |

### Git
- **커밋**: `8d518c4` fix: 업체 목록 API 쿼리 에러 수정 및 ESLint 설정 개선
- **Push**: ✅ 완료
- **브랜치**: master

### 문서 업데이트
해당 없음

### 다음 작업
- 사용자 지시 대기
